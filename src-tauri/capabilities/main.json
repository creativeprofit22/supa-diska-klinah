{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main",
  "description": "Allows the local main window to read foundation status.",
  "local": true,
  "windows": ["main"],
  "permissions": ["allow-foundation-status"],
  "platforms": ["windows"]
}
